// Placeholder boot screen. This is a bare create-vite react-ts skeleton
// scoped to G-4 (repo scaffold) -- just enough to boot on the compose
// network and satisfy the health-check proxy. The real app shell, routing,
// auth, and UI (Tailwind/shadcn, app/*, lib/*, components/ui/*,
// features/*) are built at G-6/G-7. Do not add app logic here.
function App() {
  return (
    <main>
      <h1>RAG Masterclass</h1>
      <p>Frontend boot placeholder (G-4). The real app shell lands at G-6.</p>
    </main>
  )
}

export default App
