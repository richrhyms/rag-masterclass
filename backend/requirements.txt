# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Config / validation (NFR-5: Pydantic for structured outputs; FR-BE-9: env-only config)
pydantic==2.10.4
pydantic-settings==2.7.1

# Supabase (Postgres + pgvector via PostgREST, Auth, Storage, Realtime)
supabase==2.11.0

# Auth: verify Supabase JWTs. The hosted project issues ES256 (asymmetric)
# access tokens signed with an EC (P-256) key published at the project's JWKS
# endpoint; `cryptography` is required by PyJWT to verify ES256 signatures
# (equivalent to the `PyJWT[crypto]` extra).
PyJWT==2.10.1
cryptography==44.0.0

# LLM: raw OpenAI-compatible SDK only -- NO LangChain/LangGraph/other orchestration
# frameworks (FR-BE-3, NFR-5, hard constraint). Used against OpenAI (Module 1
# Responses API) and any OpenAI-compatible Chat Completions + embeddings provider
# (Module 2 -- OpenAI, OpenRouter, Ollama, LM Studio, etc.).
# Bumped from 1.59.6 (G-4) -> 1.109.1 at G-5a: `client.responses` (Module 1)
# was not present in 1.59.6; 1.109.1 is the newest 1.x release (pre-major-bump
# to the 2.x/3.x client) and supports both Responses and Chat Completions.
openai==1.109.1

# Observability (FR-OBS-1/2; optional at runtime, absence must not break chat)
langsmith==0.2.10

# PDF text extraction (part of PRD Module 5's multi-format support, pulled
# forward for PDF only). Lightweight, no ML/layout models -- docling was
# tried first but has no PyTorch wheel available for Intel Mac + Python
# 3.13, a hard platform incompatibility, not a version-pinning issue. See
# services/ingestion.py's _default_extract for the extraction logic and
# its documented limitations (no OCR, no table-layout preservation).
pypdf==6.18.0
